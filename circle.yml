machine:
  environment:
    HUGO_VERSION: 0.31.1
    HUGO_THEME: https://github.com/Sabayon/website-future-theme

dependencies:
  override:
    - make dependencies

test:
  override:
    - make build

deployment:
  production:
    branch: master
    commands:
      - make publish
