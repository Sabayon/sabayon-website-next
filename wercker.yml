box: samueldebruyn/debian-git
build:
  steps:
    - arjen/hugo-build:
         flags: "--destination='deploy' --verbose=true"
deploy:
  steps:
    - leipert/git-push:
         gh_oauth: $GIT_TOKEN
         repo: Sabayon/sabayon-website-next
         branch: gh-pages
         basedir: deploy
